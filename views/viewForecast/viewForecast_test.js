'use strict';

describe('myApp.viewForcast module', function() {

  beforeEach(module('myApp.viewForcast'));

  describe('viewForcast controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewForcastCtrl = $controller('ViewForcastCtrl');
      expect(viewForcastCtrl).toBeDefined();
    }));

  });
});